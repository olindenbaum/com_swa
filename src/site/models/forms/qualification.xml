<?xml version="1.0" encoding="utf-8"?>
<form>
	<fieldset>

		<field name="type"
			type="sql"
			default="1"
			key_field="id"
			value_field="name"
			query="SELECT id, name FROM #__swa_qualification_type ORDER BY id ASC"
			label="Type"
			required="true"
			filter="safehtml" />

		<field name="file_upload"
			type="file"
			label="Image"
			description="Choose an image from your computer"
			size="10"
			required="true"
			accept="image/*" />

	</fieldset>
</form>
